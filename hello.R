Hello <- function ( name, age ) {
  sprintf("Hello, %s, how is your day going -- rumor has it you just turned %i years old!", name, age); 
}

numbers <- c(1,2,3)

plot(numbers)
plot(density(numbers))
hist(numbers)